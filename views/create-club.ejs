<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
	</head>
	<body>
		<%- include("partials/header", {title}) %>
		<div class="wrapper">
			<h2>Create a Secret Society</h2>
			<% if(error) { %>
			<div>
				<p><%= error %></p>
			</div>
			<%}%>
			<form action="/create-club/<%= accountId %>" method="POST">
				<div>
					<label for="name">name</label>
					<input type="text" name="name" id="name" required />
				</div>
				<div>
					<button type="submit">Create</button>
				</div>
			</form>
			<a href="/">back</a>
		</div>
	</body>
</html>

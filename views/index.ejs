<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
	</head>
	<body>
		<%- include("partials/header", {title}) %>
		<div class="wrapper">
			<h2>
				Welcome <% if(user !== undefined){ %> <%= user.name %> <% } else { %>
				Stranger <%}%>
			</h2>
		</div>
	</body>
</html>

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
	</head>
	<body>
		<%- include("partials/header", {title}) %>
		<div class="wrapper">
			<h2>Post</h2>
			<% if(error) { %>
			<div>
				<p><%= error %></p>
			</div>
			<%}%>
			<form action="/create-post/<%= clubId %>/<%= accountId %>" method="POST">
				<div>
					<input type="text" name="message" id="message" required />
				</div>
				<div>
					<button type="submit">Post</button>
				</div>
			</form>
			<a href="/club/<%= clubId %>">back</a>
		</div>
	</body>
</html>
